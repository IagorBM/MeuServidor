CREATE TABLE contatos(
	id_contatos SERIAL PRiMARY KEY,
	nome VARCHAR(100),
	endereco VARCHAR(100),
	telefone VARCHAR(100),
	email VARCHAR(100),
	celular VARCHAR(100)
);

DROP TABLE usuario

CREATE TABLE usuario(
	id_usuario SERIAL PRIMARY KEY ,
	NomeUsuario VARCHAR(100) UNIQUE ,
	senha VARCHAR(100),
	email VARCHAR(100),
	logim VARCHAR(100),
	ativo BOOLEAN
);
table usuario
INSERT INTO usuario(NomeUsuario,senha,email,logim) VALUES ('Jaime','admim0000','flamengo@gmail.com','Acessar')

SELECT nome, endereco, telefone, celular, email FROM contatos WHERE id_contatos = '2';

UPDATE contatos 
SET email = 'OiaoPorcoUIAAA@gmail.com'
WHERE id_contatos = 1;


 SELECT logim, senha FROM usuario WHERE logim='21232f297a57a5a743894a0e4a801fc3' AND senha = '202cb962ac59075b964b07152d234b70'; 
